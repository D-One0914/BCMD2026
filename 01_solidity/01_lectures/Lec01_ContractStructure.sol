// SPDX-License-Identifier: MIT

/*
    SPDX 라이센스
    
    - Solidity 0.6.8부터 SPDX 라이센스 표기 도입을 했다.
    - 스마트 컨트랙트는 블록체인에 공개되기 때문에, 코드의 사용 조건(라이센스)을 명확히 하기 위해 사용한다.
    - 주로 MIT를 사용한다.
    - SPDX 라이센스 리스트 : https://spdx.org/licenses/
*/

pragma solidity ^0.8.24;

/*
    Solidity 컴파일러 버전 설정

    특정 버전 고정
       pragma solidity 0.8.24;
       pragma solidity =0.8.24;
    
    특정 버전 이상 
       pragma solidity ^0.8.0; // 0.8.0 버전 이상만
       pragma solidity ^0.7.0; // 0.7.0 버전 이상만 (0.8.x는 포함 안 됨)
    
    버전 범위 설정
       pragma solidity >= 0.7.0 < 0.9.0;
*/

contract Lec01_ContractStructure {
    /*
        변수, 함수, 이벤트 등을 작성
    */
}